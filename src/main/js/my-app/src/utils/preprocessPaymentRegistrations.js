export default (payments, students, registrations, classes) => {
  // perform matching and return one single array with results
  // debugger;
  // 1 for each registered student get his payments. If there aren't any then you can set them.
  //if (registrations) {
    //for (let i = 0; i < registrations.length; i++ ) {
      //console.log('item:', registrations[i]);
      // debugger;
    //}
  //}

  // 2 get classes for
  // 3 
  return payments;
};
